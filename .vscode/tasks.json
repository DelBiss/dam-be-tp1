{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Start Mongo",
            "type": "shell",
            "command": "cd dev_container;service docker start;docker-compose up"
        },
        {
            "label": "Stop Mongo",
            "type": "shell",
            "command": "cd dev_container;docker-compose down;service docker stop;"
        }
    ]
}